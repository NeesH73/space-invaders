#pragma once
#include <SFML/Graphics.hpp>
#include <list>
#include <vector>

// Объявления структур
struct Player;
struct Bullet;
struct Enemy;
struct EnemyBullet;

// Функции
void runGame(sf::RenderWindow& window);